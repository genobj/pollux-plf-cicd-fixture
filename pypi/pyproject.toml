[project]
name = "plf-cicd-fixture"
version = "0.0.1"
description = "CI/CD 사슬 카나리아 — 기능 없음. 버전만 올린다."
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/plf_cicd_fixture"]

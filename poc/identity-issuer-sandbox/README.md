# PoC 3회차 임시 JWKS 게시 자리 (공개키만 · 2026-09-07 · infra)
identity-issuer v2 후보(900:18799 · sandbox)의 `jwks.json` 사본. Infisical JWT Auth 의 JWKS 모드(공개 URL 조회 · 캐시·전파)를 재기 위한 **임시 정적 게시**다 — 표준의 게시 자리는 site edge(`identity-issuer.<site>.pollux.my`). 비밀 아님(공개키). PoC 종료 시 이 디렉터리를 지운다(정본 02-secrets §4-1c-6 회수 목록).
